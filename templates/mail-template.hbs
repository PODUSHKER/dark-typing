<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <title>DARK-TYPING: Email Verification</title>
    <style>
        /* Base Styles */
        body {
            background-color: #0d0208;
            color: #00ff41;
            font-family: 'Courier New', monospace;
            margin: 0;
            padding: 0;
            line-height: 1.6;
        }

        .container {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
            border: 1px solid #003b00;
            background-color: rgba(13, 2, 8, 0.9);
        }

        .header {
            border-bottom: 1px solid #008f11;
            padding-bottom: 15px;
            margin-bottom: 25px;
            text-align: center;
        }

        h1 {
            color: #00ff41;
            font-size: 24px;
            text-shadow: 0 0 10px #00ff41;
            letter-spacing: 2px;
            margin-bottom: 10px;
        }

        /* Terminal Animation */
        .terminal-line {
            display: flex;
            margin-bottom: 15px;
            animation: terminal-flicker 0.1s infinite alternate;
        }

        .prompt {
            color: #008f11;
            margin-right: 10px;
            white-space: nowrap;
        }

        /* Verification Box */
        .verification-box {
            border: 1px solid #00ff41;
            padding: 20px;
            margin: 25px 0;
            background-color: rgba(0, 255, 65, 0.05);
            text-align: center;
        }

        /* Button Styles */
        .verify-btn {
            display: inline-block;
            background-color: #00ff41;
            color: #0d0208;
            text-decoration: none;
            padding: 12px 30px;
            font-weight: bold;
            border: none;
            border-radius: 3px;
            margin: 15px 0;
            font-family: 'Courier New', monospace;
            font-size: 16px;
            text-transform: uppercase;
            letter-spacing: 1px;
            position: relative;
            overflow: hidden;
            transition: all 0.3s;
        }

        .verify-btn:hover {
            background-color: #008f11;
            color: #00ff41;
            box-shadow: 0 0 15px #00ff41;
        }

        .verify-btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(0, 255, 65, 0.4), transparent);
            transition: 0.5s;
        }

        .verify-btn:hover::before {
            left: 100%;
        }

        /* Code Display */
        .code-container {
            margin: 25px 0;
            text-align: center;
        }

        .verification-code {
            display: inline-block;
            font-size: 24px;
            letter-spacing: 5px;
            padding: 10px 20px;
            background-color: rgba(0, 255, 65, 0.1);
            border: 1px dashed #00ff41;
            margin: 10px 0;
        }

        /* Footer */
        .footer {
            margin-top: 30px;
            font-size: 12px;
            color: #008f11;
            text-align: center;
            border-top: 1px solid #003b00;
            padding-top: 15px;
        }

        /* Animations */
        @keyframes terminal-flicker {
            0% {
                opacity: 0.95
            }

            100% {
                opacity: 1
            }
        }

        @keyframes pulse {
            0% {
                box-shadow: 0 0 5px #00ff41
            }

            100% {
                box-shadow: 0 0 15px #00ff41
            }
        }

        .pulse {
            animation: pulse 2s infinite alternate;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="header">
            <h1>DARK-TYPING VERIFICATION</h1>
            <div class="terminal-line">
                <span class="prompt">></span>
                <span>Secure access protocol initiated</span>
            </div>
        </div>

        <div class="terminal-line">
            <span class="prompt">></span>
            <span>Identity verification required for terminal access</span>
        </div>

        <div class="verification-box pulse">
            <h2>EMAIL VERIFICATION REQUIRED</h2>
            <p>To complete your registration, please verify your email address:</p>

            <!-- Основная кнопка подтверждения -->
            <a href="{{verificationLink}}" class="verify-btn">CONFIRM IDENTITY</a>

        </div>

        <div class="terminal-line">
            <span class="prompt">></span>
            <span>Security notice: This link will expire in 24 hours</span>
        </div>

        <div class="terminal-line">
            <span class="prompt">></span>
            <span>If you didn't request this, please ignore this message</span>
        </div>

        <div class="footer">
            <p>DARK-TYPING SYSTEM • SECURE TERMINAL ACCESS</p>
            <p>© {{currentYear}} All rights reserved</p>
        </div>
    </div>
</body>

</html>